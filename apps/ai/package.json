{
  "name": "@maidan/ai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "uv run python -m compileall src",
    "lint": "uv run ruff check .",
    "start": "uv run uvicorn maidan_ai.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "test": "uv run pytest",
    "typecheck": "uv run mypy src tests"
  }
}
